# Proposal: 看板中间执行回合折叠

## 变更动机
看板页面中对于执行次数较多的卡片，其下方的 Turns 气泡堆叠过多，导致看板卡片高度过长，降低了看板的整洁度与可读性。

## 目标
实现中间执行回合（Turn 1 ~ Turn N-1）的折叠展示功能。默认仅直显最新的 Turn，在需要审计历史时，允许用户通过点击折叠按钮一键展开之前的步骤。

## 范围
- 仅修改前端 `index.html` 中的 CSS 样式区与 Turns 渲染模板逻辑。
- 逻辑仅在卡片 Turns 数量大于 1 时生效。
